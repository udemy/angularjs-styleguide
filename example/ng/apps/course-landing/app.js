define(['angular', './discovery/module'],
    function (angular) {
        'use strict';

        angular.module('apps.courseLanding', [
            'apps.courseLanding.discovery'
        ]);
    });
